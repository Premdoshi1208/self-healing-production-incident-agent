global:
  resolve_timeout: 1m

route:
  receiver: "ai-sre-webhook"
  group_wait: 5s
  group_interval: 30s
  repeat_interval: 30m

receivers:
  - name: "ai-sre-webhook"
    webhook_configs:
      - url: "http://host.docker.internal:8000/api/prometheus/webhook"
        send_resolved: false